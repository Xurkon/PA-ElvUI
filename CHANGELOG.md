# Changelog

## v1.3.8

### Fixes
- **ElvUI_Enhanced**: Fixed compatibility with updated OmniBar.
    - Added support for OmniBar's new `bars` table structure (replacing the deprecated `icons` table).
    - Resolved `bad argument #1 to 'ipairs'` error that caused crashes on login.
